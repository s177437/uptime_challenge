#!/bin/bash
docker run -d -P --name worker8 worker
docker run -d -P --name worker9 worker
docker run -d -P --name worker10 worker
docker run -d -P --name worker11 worker
docker run -d -P --name worker12 worker
docker run -d -P --name worker13 worker
docker run -d -P --name worker14 worker
